{
  "name": "loki-builder",
  "version": "0.1.0",
  "description": "",
  "type": "module",
  "license": "MIT",
  "scripts": {
    "deep": "pnpm --recursive --bail --color --reporter append-only",
    "start": "pnpm deep start",
    "dev": "pnpm deep dev",
    "build": "pnpm deep build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
